/**
 * NFTCollection contract for NFT Launchpad.
 * Bytecode: run `npx hardhat compile` then copy from
 * artifacts/contracts/NFTCollection.sol/NFTCollection.json -> "bytecode" (hex string without 0x).
 * Leave empty until compiled; hook will throw a clear error if missing.
 */
export const NFT_COLLECTION_BYTECODE = ''
// Example after compile: export const NFT_COLLECTION_BYTECODE = '608060405234801561000f...'
